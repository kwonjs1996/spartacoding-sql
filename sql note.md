```sql
show tables
데이터베이스 안에 테이블을 보여줘라


select *
모든필드 
select * from orders

모든필드 from 어디서 orders

select order_no, created_at, user_id, email from orders
 order_no, created_at, user_id, email 필드에서 
```
